# Secrets / local files
.env
betterposters.db
data/

# Prototype / Maintainerr integration (work in progress, not pushed)
overlay/

# v2 multi-server version (local only, not pushed)
v2/

# Local utility (not pushed to GitHub)
force_update.py

# Build config (kept out of the public repo)
uv.lock
pyproject.toml

# Python
__pycache__/
*.py[cod]
.venv/

# IDE
.idea/
